<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ .Title }}</title>

  {{/* Pull in the theme’s CSS if it exists, otherwise your page still shows */}}
  {{ with resources.Get "css/main.css" }}<link rel="stylesheet" href="{{ .RelPermalink }}">{{ end }}
  {{ with resources.Get "css/custom.css" }}<link rel="stylesheet" href="{{ .RelPermalink }}">{{ end }}

  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
           max-width: 1100px; margin: 32px auto; padding: 0 16px; line-height: 1.6; }
    .btn { display:inline-block; padding:10px 14px; border:1px solid #999; border-radius:10px;
           text-decoration:none; font-weight:600; color:inherit; }
    iframe { width: 100%; height: 900px; border: 1px solid #ddd; border-radius: 8px; }
  </style>
</head>
<body>
  <a class="btn" href="{{ "/" | relURL }}">← Back to Portfolio</a>
  <h1>{{ .Title }}</h1>

  <div>
    {{ .Content }}
  </div>
</body>
</html>
